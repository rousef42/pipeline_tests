platform: linux

image_resource:
  type: docker-image
  source:
    repository: frank1rouse/test-build-service
    tag: latest

inputs:
  - name: source-code

run:
  path: sh
  args: 
  - -exc
  - |
    env
    docker ps

