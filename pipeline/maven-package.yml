platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker
    tag: latest
    privileged: true

inputs:
  - name: source-code

run:
  path: sh
  args: 
  - -exc
  - |
    env
    whoami
    ls -l source-code/pipeline/launch_docker.sh
    source-code/pipeline/launch_docker.sh
    docker ps

