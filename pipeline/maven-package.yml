platform: linux

image_resource:
  type: docker-image
  source:
    repository: frank1rouse/test-build-service
    tag: latest

inputs:
  - name: source-code

run:
  path: sh
  args: 
  - -exc
  - |
    env
    whoami
    ls -l source-code/pipeline/launch_docker.sh
    source-code/pipeline/launch_docker.sh
    docker ps

